//Структуры данных 

//1) Очередь


const graph = {}

graph.a = ['b', 'c']
graph.b = ['f']
graph.c = ['d', 'e']
graph.d = ['f']
graph.e = ['f']
graph.f = ['g']

const breadSearch = (graph, start, end) => {
    let queue = []
    queue.push(start)
    while(queue.length > 0) {

    }
}